name: example
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_tutorial: 
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  golden_toolkit: ^0.12.0

flutter:
  uses-material-design: true